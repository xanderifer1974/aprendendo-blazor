@page "/render-fragment-content"

<Card CardTitle="RenderFragment (Content)" BodyCard="Neste exemplo estamos utilizando o RenderFragment, onde indicamos duas seções distintas."/>
<Card CardTitle="RenderFragmento (ChildContent)" BodyCard="No caso do exemplo anterior, utilizamos o RenderFragment, porém com uma única seção. Neste caso utilizamos o ChildContent"> </Card>

@code {

}
